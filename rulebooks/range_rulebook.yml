---
- name: Range Event Rulebook
  hosts: localhost
  sources:
    - ansible.eda.range:
        limit: 30

  rules:
    - name: Print message when value is 27
      condition: event.payload.value == 27
      action:
        debug:
          msg: "I am learning to write a rulebook as a part of training"

